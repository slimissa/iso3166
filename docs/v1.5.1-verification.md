# v1.5.1 verification

Date: 2026-09-23
Commit: 47509045ab5834ad680bed3eaebd30be22be4659
Tag: v1.5.1 (points at 47509045ab5834ad680bed3eaebd30be22be4659)

## What changed

`enrich_field.py` supports `--field subregion` and
`--field intermediate_region`, and treats both as scalars. The
intermediate snapshot is populated with seven UN M49 values,
replacing the placeholder.

No change to `iso3166.json` data beyond meta.version and
meta.updated.

## Coverage

| Item | v1.5.0 | v1.5.1 |
|------|--------|--------|
| `enrich_field.py` fields | 5 | 7 |
| Scalar-aware fields | 0 | 2 |
| Intermediate snapshot values | 0 (stub) | 7 |

## CI at the tag

Workflow: .github/workflows/validate.yml
Code CI ran on the release commit with `completed success`.

## Known limitations

- Neither new field is added to `check-fields`. Both have
  legitimate null values across the registry.
- The ISO 4217 mirror remains outstanding in that repository.
- The `--dry-run` output for a scalar field prints the string
  without quotes, so a scalar and a one-element list look similar.
  Cosmetic only; the written value is correct.
