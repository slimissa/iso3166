# v1.5.0 verification

Date: 2026-09-23
Commit: c329b66cd9ab1ef7095993bc6835a33b4e3492b5
Tag: v1.5.0

## What changed

Fixture support and documentation for `intermediate_region`. The
version bump to 1.5.0. No data change: `intermediate_region` was
already complete at 105/252 from the v1.0.0 UN M49 import.

## Coverage

| Field | v1.4.0 | v1.5.0 |
|-------|--------|--------|
| `intermediate_region` | 105 / 252 | 105 / 252 (no change) |

## CI at the tag

Workflow: .github/workflows/validate.yml

Code CI ran on 497fd17 with `completed success`. The three commits
since (scope document, CHANGELOG, record correction) are
documentation-only and do not trigger CI by design
(`paths-ignore` includes `docs/**` and `**/*.md`).

## Known limitations

- `tools/enrich_field.py` does not support `--field subregion` or
  `--field intermediate_region`. Both fields are populated from the
  v1.0.0 UN M49 import and are not tool-managed. Tracked for a
  future release.
- `tools/m49_intermediate_region_snapshot.json` is a stub. The
  field is validated by the schema pattern check, not by a snapshot
  cross-reference.
- The ISO 4217 mirror remains outstanding in that repository. Not
  in scope for this release.
